/* ============================================================
   OM Produce — deploy-time runtime config (GENERATED)
   ------------------------------------------------------------
   This committed stub is intentionally EMPTY so the repo holds no secrets.
   On Vercel, the build (scripts/gen-env.js) OVERWRITES this file with values
   from environment variables — notably OM_API_TOKEN — and config.js merges
   them onto window.OM_CONFIG. Do NOT commit a filled-in version of this file.
   ============================================================ */
window.OM_ENV = {};
